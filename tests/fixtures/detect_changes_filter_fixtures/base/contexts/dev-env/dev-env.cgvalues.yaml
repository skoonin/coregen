# Base: Local Dev Environment configuration
environment: dev
region: local
active: true

components:
  postgres:
    config:
      active: true
      required: false
      priority: 2
  # Note: redis doesn't exist in base, will be NEW in current
